<section class="tab notes {{tabs.notes.cssClass}}" data-group="primary" data-tab="notes" data-application-part="notes">
    <prose-mirror 
    name="system.notes" 
    value="{{system.notes}}" 
    document-uuid="{{actor.uuid}}"
    toggled
    {{#unless editable}}disabled{{/unless}}>
    {{{notes.enriched}}}
    </prose-mirror>
</section>